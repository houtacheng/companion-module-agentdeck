{
  "name": "companion-module-agentdeck",
  "version": "1.18.0",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "package": "npm run build && companion-module-build",
    "test": "vitest run",
    "embed-codex-pet": "node scripts/embed-codex-pet.mjs"
  },
  "license": "MIT",
  "engines": {
    "node": "^22.20"
  },
  "dependencies": {
    "@companion-module/base": "~2.1.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@companion-module/tools": "^3.0.1",
    "@types/node": "^22.19.0",
    "@types/ws": "^8.5.13",
    "rimraf": "^6.0.1",
    "typescript": "~5.9.0",
    "vitest": "^2.1.0"
  }
}
